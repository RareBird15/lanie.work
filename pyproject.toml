[project]
name = "lanie-work"
version = "0.1.0"
description = "Personal blog and portfolio built with Hugo, with Python helper scripts for publishing."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "feedparser>=6.0.11",
  "python-dotenv>=1.2.2",
  "requests>=2.33.1",
]

[dependency-groups]
dev = ["pre-commit>=4.5.1"]
